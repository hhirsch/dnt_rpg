# it's a comment
# another comment

script()
   int a b
   a = 2
   b = 10
   a=b + a
   int c
   c = (a + b)*20+30;
   bool variavel
   variavel = false
   c = c / 10
   if( (c > 40) && (a > 10) && (!variavel) )
      int t
   else
      int f
   end
end

